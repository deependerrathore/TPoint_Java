public class Constructor {
	/*constructor have same name as class.Every class has a constructor.
	 * Each time a new object is created , at least one constructor will be invoked.
	 * A class can have more than on constructor.
	 */
	public Constructor(){
		
	}
	public Constructor(String name){
		//This contructor take one parameter, name
	}
}
